<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyh.simple.demo.dao.PersonDao">

    <select id="getPersonInfo" resultType="com.lyh.common.entity.PersonModel">
        select * from t_person where id = #{id}
    </select>

    <insert id="addPersonInfo">
        insert into t_person(createTime, userName, age, personName)
        values(#{param.createTime}, #{param.userName}, #{param.age}, #{param.personName})
    </insert>

    <update id="updatePersonAge">
        update t_person
        set age = age + 1
        where id = #{id}
    </update>

</mapper>