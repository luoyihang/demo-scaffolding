<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyh.sharding.table.dao.PersonDao">

    <select id="getPersonByUserName" resultType="java.lang.String">
        select personName from t_person where userName = #{userName}
    </select>

    <select id="getPersonByUserName2" resultType="java.lang.String">
        select personName from t_person where userName = "123"
    </select>

    <select id="listPersonByUserName" resultType="java.lang.String">
        select personName from t_person
        where userName in
        <foreach collection="userNameList"  item="item" open="(" close=")" separator=",">
            #{item}
        </foreach>
    </select>

    <select id="getPersonRangeByUserNameCreateTime" resultType="java.lang.String">
        select personName from t_person_range
        where userName = #{userName}
        and createTime between #{startTime} and #{endTime}
        <!--        <![CDATA[ and createTime >= #{startTime} ]]>-->
        <!--        <![CDATA[ and createTime <= #{endTime} ]]>-->
    </select>

    <select id="listPersonRangeByUserNameCreateTime" resultType="java.lang.String">
        select personName from t_person_range
        where userName in
        <foreach collection="list" item="item" separator="," open="(" close=")">#{item}</foreach>
        and createTime between #{startTime} and #{endTime}
    </select>

    <insert id="addPerson">
        insert into t_person(createTime, userName, age, personName)
        values(#{personModel.createTime}, #{personModel.userName}, #{personModel.age}, #{personModel.personName})
    </insert>

    <insert id="addPersonList">
        insert into t_person(createTime, userName, age, personName)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.createTime}, #{item.userName}, #{item.age}, #{item.personName})
        </foreach>
    </insert>

    <insert id="addPersonRange">
        insert into t_person_range(createTime, userName, age, personName)
        values(now(), #{userName}, #{age}, #{personName})
    </insert>

    <insert id="addPersonRangeList">
        <foreach collection="list" item="item" separator=";">
            insert into t_person_range(createTime, userName, age, personName)
            values(now(), #{item.userName}, #{item.age}, #{item.personName})
            ON duplicate key update
            age = age + 1
        </foreach>
    </insert>

</mapper>